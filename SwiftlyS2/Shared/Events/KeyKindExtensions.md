# 📦 KeyKindExtensions

Provides extension methods for <see cref="KeyKind"/>.

**命名空间:** `SwiftlyS2.Shared.Events`

**类型:** `class`

## ⚙️ 方法

### ToKeyBind (静态)

```csharp
KeyBind ToKeyBind(this KeyKind keyKind)
```

Converts a <see cref="KeyKind"/> enum value to its corresponding <see cref="KeyBind"/> enum value.

**参数:**

- `keyKind` (`this KeyKind`) - The <see cref="KeyKind"/> value to convert.

**返回值:** `KeyBind` - The corresponding <see cref="KeyBind"/> enum value.

