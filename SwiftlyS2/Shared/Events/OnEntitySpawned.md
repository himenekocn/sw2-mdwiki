# 🔗 OnEntitySpawned

Called when an entity is spawned.

**命名空间:** `SwiftlyS2.Shared.Events`

**类型:** `delegate`

**委托:**
```csharp
delegate void OnEntitySpawned(IOnEntitySpawnedEvent @event);
```

**参数:**

- `@event` (`IOnEntitySpawnedEvent`)

