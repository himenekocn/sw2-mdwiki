# 📦 BitFieldHelper

**命名空间:** `SwiftlyS2.Shared.Misc`

**类型:** `class`

## ⚙️ 方法

### GetBits (静态)

```csharp
int GetBits(ref byte data, int index, int bitCount)
```

**参数:**

- `data` (`ref byte`)
- `index` (`int`)
- `bitCount` (`int`)

**返回值:** `int`

### SetBits (静态)

```csharp
void SetBits(ref byte data, int index, int bitCount, int value)
```

**参数:**

- `data` (`ref byte`)
- `index` (`int`)
- `bitCount` (`int`)
- `value` (`int`)

### GetBits (静态)

```csharp
int GetBits(ref int data, int index, int bitCount)
```

**参数:**

- `data` (`ref int`)
- `index` (`int`)
- `bitCount` (`int`)

**返回值:** `int`

### SetBits (静态)

```csharp
void SetBits(ref int data, int index, int bitCount, int value)
```

**参数:**

- `data` (`ref int`)
- `index` (`int`)
- `bitCount` (`int`)
- `value` (`int`)

### GetBits (静态)

```csharp
long GetBits(ref long data, int index, int bitCount)
```

**参数:**

- `data` (`ref long`)
- `index` (`int`)
- `bitCount` (`int`)

**返回值:** `long`

### SetBits (静态)

```csharp
void SetBits(ref long data, int index, int bitCount, long value)
```

**参数:**

- `data` (`ref long`)
- `index` (`int`)
- `bitCount` (`int`)
- `value` (`long`)

