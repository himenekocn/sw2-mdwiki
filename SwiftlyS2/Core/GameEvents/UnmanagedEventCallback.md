<a id="unmanagedeventcallback"></a>

# 🔗 UnmanagedEventCallback

**命名空间:** `SwiftlyS2.Core.GameEvents`

**类型:** `delegate`

**委托:**
```csharp
delegate HookResult UnmanagedEventCallback(uint hash, nint pEvent, nint pDontBroadcast);
```

**参数:**

- `hash` (`uint`)
- `pEvent` (`nint`)
- `pDontBroadcast` (`nint`)

**返回值:** `HookResult`

