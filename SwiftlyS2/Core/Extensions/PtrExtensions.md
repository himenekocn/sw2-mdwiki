# 📦 PtrExtensions

**命名空间:** `SwiftlyS2.Core.Extensions`

**类型:** `class`

## ⚙️ 方法

### CopyFrom (静态)

```csharp
void CopyFrom(this nint ptr, byte[] source)
```

**参数:**

- `ptr` (`this nint`)
- `source` (`byte[]`)

### CopyFrom (静态)

```csharp
void CopyFrom(this nint ptr, nint source, int size)
```

**参数:**

- `ptr` (`this nint`)
- `source` (`nint`)
- `size` (`int`)

### CopyFrom (静态)

```csharp
void CopyFrom(this nint ptr, int offset, nint source, int size)
```

**参数:**

- `ptr` (`this nint`)
- `offset` (`int`)
- `source` (`nint`)
- `size` (`int`)

### IsValidPtr (静态)

```csharp
bool IsValidPtr(this nint ptr)
```

**参数:**

- `ptr` (`this nint`)

**返回值:** `bool`

