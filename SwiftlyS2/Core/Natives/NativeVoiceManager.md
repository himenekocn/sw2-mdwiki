# 📦 NativeVoiceManager

**命名空间:** `SwiftlyS2.Core.Natives`

**类型:** `class`

## ⚙️ 方法

### SetClientListenOverride (静态)

```csharp
void SetClientListenOverride(int playerid, int targetid, int listenOverride)
```

**参数:**

- `playerid` (`int`)
- `targetid` (`int`)
- `listenOverride` (`int`)

### GetClientListenOverride (静态)

```csharp
int GetClientListenOverride(int playerid, int targetid)
```

**参数:**

- `playerid` (`int`)
- `targetid` (`int`)

**返回值:** `int`

### SetClientVoiceFlags (静态)

```csharp
void SetClientVoiceFlags(int playerid, int flags)
```

**参数:**

- `playerid` (`int`)
- `flags` (`int`)

### GetClientVoiceFlags (静态)

```csharp
int GetClientVoiceFlags(int playerid)
```

**参数:**

- `playerid` (`int`)

**返回值:** `int`

