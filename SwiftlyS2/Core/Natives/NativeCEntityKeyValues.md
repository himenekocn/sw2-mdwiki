# 📦 NativeCEntityKeyValues

**命名空间:** `SwiftlyS2.Core.Natives`

**类型:** `class`

## ⚙️ 方法

### Allocate (静态)

```csharp
nint Allocate()
```

**返回值:** `nint`

### Deallocate (静态)

```csharp
void Deallocate(nint keyvalues)
```

**参数:**

- `keyvalues` (`nint`)

### GetBool (静态)

```csharp
bool GetBool(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `bool`

### GetInt (静态)

```csharp
int GetInt(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `int`

### GetUint (静态)

```csharp
uint GetUint(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `uint`

### GetInt64 (静态)

```csharp
long GetInt64(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `long`

### GetUint64 (静态)

```csharp
ulong GetUint64(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `ulong`

### GetFloat (静态)

```csharp
float GetFloat(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `float`

### GetDouble (静态)

```csharp
double GetDouble(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `double`

### GetString (静态)

```csharp
string GetString(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `string`

### GetPtr (静态)

```csharp
nint GetPtr(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `nint`

### GetStringToken (静态)

```csharp
CUtlStringToken GetStringToken(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `CUtlStringToken`

### GetColor (静态)

```csharp
Color GetColor(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `Color`

### GetVector (静态)

```csharp
Vector GetVector(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `Vector`

### GetVector2D (静态)

```csharp
Vector2D GetVector2D(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `Vector2D`

### GetVector4D (静态)

```csharp
Vector4D GetVector4D(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `Vector4D`

### GetQAngle (静态)

```csharp
QAngle GetQAngle(nint keyvalues, string key)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)

**返回值:** `QAngle`

### SetBool (静态)

```csharp
void SetBool(nint keyvalues, string key, bool value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`bool`)

### SetInt (静态)

```csharp
void SetInt(nint keyvalues, string key, int value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`int`)

### SetUint (静态)

```csharp
void SetUint(nint keyvalues, string key, uint value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`uint`)

### SetInt64 (静态)

```csharp
void SetInt64(nint keyvalues, string key, long value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`long`)

### SetUint64 (静态)

```csharp
void SetUint64(nint keyvalues, string key, ulong value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`ulong`)

### SetFloat (静态)

```csharp
void SetFloat(nint keyvalues, string key, float value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`float`)

### SetDouble (静态)

```csharp
void SetDouble(nint keyvalues, string key, double value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`double`)

### SetString (静态)

```csharp
void SetString(nint keyvalues, string key, string value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`string`)

### SetPtr (静态)

```csharp
void SetPtr(nint keyvalues, string key, nint value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`nint`)

### SetStringToken (静态)

```csharp
void SetStringToken(nint keyvalues, string key, CUtlStringToken value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`CUtlStringToken`)

### SetColor (静态)

```csharp
void SetColor(nint keyvalues, string key, Color value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`Color`)

### SetVector (静态)

```csharp
void SetVector(nint keyvalues, string key, Vector value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`Vector`)

### SetVector2D (静态)

```csharp
void SetVector2D(nint keyvalues, string key, Vector2D value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`Vector2D`)

### SetVector4D (静态)

```csharp
void SetVector4D(nint keyvalues, string key, Vector4D value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`Vector4D`)

### SetQAngle (静态)

```csharp
void SetQAngle(nint keyvalues, string key, QAngle value)
```

**参数:**

- `keyvalues` (`nint`)
- `key` (`string`)
- `value` (`QAngle`)

